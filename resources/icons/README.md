# Icons

Place application icons here:

- `app.ico` — Windows (256x256 multi-resolution)
- `app.icns` — macOS
- `app_icon.png` — Source PNG (512x512)

Generate from source PNG:
- Windows: Use an online ICO converter or ImageMagick
- macOS: `iconutil -c icns app.iconset`
